'use strict';
import most from 'most';

const h = require('snabbdom/h');

const init = (dispatch) => {
  // stream.filter();
};

const render = (props) => {
  return h('div', 'counters');
};

module.exports = {init, render};
